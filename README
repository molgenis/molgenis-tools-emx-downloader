# MOLGENIS Downloader
This tool allows you to download data from a MOLGENIS instance in the MOLGENIS
EMX format.

## Usage
usage: java -jar downloader.jar [options] [entity1 [entity2 [entity3] …]]
 -I,--insecureSSL             Ignore SSL certicate chain errors and
                              hostname mismatches.
 -m,--meta                    Write the metadata for the entities to the
                              output file.
 -o,--outFile <Output file>   Name of the file to write the data to.
 -P,--password <Password>     Password for the MOLGENIS user to login
 -u,--url <URL>               URL of the MOLGENIS instance
 -U,--user <Username>         MOLGENIS username to login with to download
                              the data.
